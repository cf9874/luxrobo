export enum teamAuth {
  // 이후 api 개발에 따라 수정
  Administrator = 1,
  TeamMember,
}
export enum TEAMAUTH_TITLE {
  Administrator = "Administrator",
  TeamMember = "Team Member",
}

export const Auth = ["Administrator", "Team Member"]
